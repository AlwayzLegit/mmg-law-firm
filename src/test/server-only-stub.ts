// Test stub for the `server-only` package. In a real Next.js build,
// importing `server-only` from a client graph throws; under vitest (plain
// Node) there's no such graph, so we alias it to this empty module so that
// server-side modules can be unit-tested in isolation.
export {};
